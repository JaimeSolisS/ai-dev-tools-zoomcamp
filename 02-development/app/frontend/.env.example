# Which backend implementation the services layer uses: "mock" (default) or "http".
VITE_BACKEND=mock
# Simulated network latency for the mock backend, in milliseconds.
VITE_MOCK_LATENCY_MS=120
# Base URL for the real backend (used when VITE_BACKEND=http).
VITE_API_BASE_URL=http://localhost:8091
